# arena

Generate several independent candidates for the same non-trivial artifact, judge them against a concrete rubric, and synthesize one verified result.

## Install

```bash
bunx skills@latest add petalas/skills --skill arena -g -y
```

## Usage

```text
Use $arena for this task.
```

This plugin adapts material from pstack. The installed skill includes the full upstream notice and exact provenance.
