# $gpt-image-2-5

Apply the `gpt-image-2-5` skill for image generation or editing through a ChatGPT subscription. Follow its route-selection and exact-model checks first. Prepare an image prompt from the request, preserving any explicitly supplied image prompt. Use the available subscription-backed image tool or the bundled CLI fallback, then inspect the saved artifact and record its manifest. Display the result and report model evidence without inferring 2.5 from this command's name. The skill does not execute a billed API fallback.
