# $gpt-image-2-5

Apply the `gpt-image-2-5` skill when the user asks for GPT Image 2.5, ChatGPT Images 2.5, or image generation or editing through their ChatGPT subscription. Run the bundled Python script with the user's prompt unchanged. Keep the result at the user's named path, or use the timestamped default when no path was named. Verify the output file exists before reporting success, then display or attach it. Never switch to an API-key route or another image model.
