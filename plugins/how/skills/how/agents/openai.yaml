interface:
  display_name: "how"
  short_description: "Explain how code works: subsystem architecture, runtime flow, ownership, placement, and layering. Use why for historical motivation."
  default_prompt: "Use $how for this task."

policy:
  allow_implicit_invocation: false
