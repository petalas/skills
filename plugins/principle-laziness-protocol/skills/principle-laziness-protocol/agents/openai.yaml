interface:
  display_name: "principle-laziness-protocol"
  short_description: "Apply when refactoring, evaluating diff size, or tempted to add abstractions, layers, or signal threading. Bias toward deletion and the smallest change that solves the problem."
  default_prompt: "Use $principle-laziness-protocol for this task."
