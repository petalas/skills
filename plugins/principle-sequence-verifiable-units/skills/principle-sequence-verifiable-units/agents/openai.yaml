interface:
  display_name: "principle-sequence-verifiable-units"
  short_description: "Apply to multi-step work such as sweeps, migrations, and runs of similar edits. Break work into small units that each end in a verifiable state, check each before the next, and order authorized delivery so the sequence proves itself."
  default_prompt: "Use $principle-sequence-verifiable-units for this task."
