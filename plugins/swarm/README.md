# swarm

Fan out independent subagents across parallel slices or races, drain every required result, and return one consolidated local report.

## Install

```bash
bunx skills@latest add petalas/skills --skill swarm -g -y
```

## Usage

```text
Use $swarm for this task.
```

This plugin adapts material from pstack. The installed skill includes the full upstream notice and exact provenance.
